# Application Configuration
APP_ENV=development
LOG_LEVEL=INFO

# Database Configuration
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/shared_accounts

# Redis Configuration
# 无密码格式: redis://localhost:6379/0
# 有密码格式: redis://:your-password@localhost:6379/0
# 带用户名和密码: redis://username:password@localhost:6379/0
REDIS_URL=redis://localhost:6379/0

# JWT Configuration
JWT_SECRET_KEY=your-secret-key-change-this-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRE_HOURS=24

# Refresh Token Configuration
# Refresh Token 过期时间（天），默认 7 天
REFRESH_TOKEN_EXPIRE_DAYS=7
# Refresh Token 密钥（可选，默认使用 JWT_SECRET_KEY）
# 建议在生产环境中使用不同的密钥以增强安全性
# REFRESH_TOKEN_SECRET_KEY=your-refresh-token-secret-key

# Plug-in API Configuration
PLUGIN_API_BASE_URL=http://localhost:8045
PLUGIN_API_ADMIN_KEY=sk-admin-your-admin-key-here
PLUGIN_API_ENCRYPTION_KEY=your-encryption-key-here-min-32-chars

# Codex Configuration (Optional)
# Override the Codex model list returned by `/api/codex/models` and `/v1/models` (when X-Api-Type: codex).
# Format: JSON array or comma-separated string.
# Example: ["gpt-5.2-codex","gpt-5.2","gpt-5.1-codex-max"]
# CODEX_SUPPORTED_MODELS=
#
# Optional: override Codex upstream base URL (default: https://chatgpt.com/backend-api/codex)
# CODEX_API_BASE_URL=
#
# Optional: outbound proxy for Codex/OpenAI upstream requests.
# Recommended: HTTP proxy (works out-of-the-box). Example:
#   CODEX_PROXY_URL=http://127.0.0.1:7890
# If you run backend in Docker and your proxy is on the host, use:
#   CODEX_PROXY_URL=http://host.docker.internal:7890
# CODEX_PROXY_URL=

# Admin Account Configuration (Optional)
# 管理员账号配置（可选，首次启动时自动创建）
# 如果不需要自动创建管理员，可以留空或删除这两行
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your-strong-password-here
